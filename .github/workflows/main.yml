name: Build and Push to Harbor
on: [push]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      
      - name: Build image
        run: docker build -t registry.local/myapp/web-page:${{ github.sha }} .
      
      - name: Login to Harbor
        run: echo ${{ secrets.HARBOR_PASSWORD }} | docker login registry.local -u admin --password-stdin
      
      - name: Push image
        run: |
          docker push registry.local/myapp/web-page:${{ github.sha }}
          docker tag registry.local/myapp/web-page:${{ github.sha }} registry.local/myapp/web-page:latest
          docker push registry.local/myapp/web-page:latest
